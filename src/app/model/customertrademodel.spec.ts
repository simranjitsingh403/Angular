import { Customertrademodel } from './customertrademodel';

describe('Customertrademodel', () => {
  it('should create an instance', () => {
    expect(new Customertrademodel()).toBeTruthy();
  });
});
