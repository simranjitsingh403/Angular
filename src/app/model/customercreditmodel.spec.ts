import { Customercreditmodel } from './customercreditmodel';

describe('Customercreditmodel', () => {
  it('should create an instance', () => {
    expect(new Customercreditmodel()).toBeTruthy();
  });
});
